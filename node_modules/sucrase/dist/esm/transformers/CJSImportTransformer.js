


import {IdentifierRole, isDeclaration, isObjectShorthandDeclaration} from "../parser/tokenizer";
import {ContextualKeyword} from "../parser/tokenizer/keywords";
import {TokenType as tt} from "../parser/tokenizer/types";

import elideImportEquals from "../util/elideImportEquals";
import getDeclarationInfo, {

  EMPTY_DECLARATION_INFO,
} from "../util/getDeclarationInfo";
import getImportExportSpecifierInfo from "../util/getImportExportSpecifierInfo";
import isExportFrom from "../util/isExportFrom";
import {removeMaybeImportAttributes} from "../util/removeMaybeImportAttributes";
import shouldElideDefaultExport from "../util/shouldElideDefaultExport";


import Transformer from "./Transformer";

/**
 * Class for editing import statements when we are transforming to commonjs.
 */
export default class CJSImportTransformer extends Transformer {
   __init() {this.hadExport = false}
   __init2() {this.hadNamedExport = false}
   __init3() {this.hadDefaultExport = false}
  

  constructor(
     rootTransformer,
     tokens,
     importProcessor,
     nameManager,
     helperManager,
     reactHotLoaderTransformer,
     enableLegacyBabel5ModuleInterop,
     enableLegacyTypeScriptModuleInterop,
     isTypeScriptTransformEnabled,
     isFlowTransformEnabled,
     preserveDynamicImport,
     keepUnusedImports,
  ) {
    super();this.rootTransformer = rootTransformer;this.tokens = tokens;this.importProcessor = importProcessor;this.nameManager = nameManager;this.helperManager = helperManager;this.reactHotLoaderTransformer = reactHotLoaderTransformer;this.enableLegacyBabel5ModuleInterop = enableLegacyBabel5ModuleInterop;this.enableLegacyTypeScriptModuleInterop = enableLegacyTypeScriptModuleInterop;this.isTypeScriptTransformEnabled = isTypeScriptTransformEnabled;this.isFlowTransformEnabled = isFlowTransformEnabled;this.preserveDynamicImport = preserveDynamicImport;this.keepUnusedImports = keepUnusedImports;CJSImportTransformer.prototype.__init.call(this);CJSImportTransformer.prototype.__init2.call(this);CJSImportTransformer.prototype.__init3.call(this);;
    this.declarationInfo = isTypeScriptTransformEnabled
      ? getDeclarationInfo(tokens)
      : EMPTY_DECLARATION_INFO;
  }

  getPrefixCode() {
    let prefix = "";
    if (this.hadExport) {
      prefix += 'Object.defineProperty(exports, "__esModule", {value: true});';
    }
    return prefix;
  }

  getSuffixCode() {
    if (this.enableLegacyBabel5ModuleInterop && this.hadDefaultExport && !this.hadNamedExport) {
      return "\nmodule.exports = exports.default;\n";
    }
    return "";
  }

  process() {
    // TypeScript `import foo = require('foo');` should always just be translated to plain require.
    if (this.tokens.matches3(tt._import, tt.name, tt.eq)) {
      return this.processImportEquals();
    }
    if (this.tokens.matches1(tt._import)) {
      this.processImport();
      return true;
    }
    if (this.tokens.matches2(tt._export, tt.eq)) {
      this.tokens.replaceToken("module.exports");
      return true;
    }
    if (this.tokens.matches1(tt._export) && !this.tokens.currentToken().isType) {
      this.hadExport = true;
      return this.processExport();
    }
    if (this.tokens.matches2(tt.name, tt.postIncDec)) {
      // Fall through to normal identifier matching if this doesn't apply.
      if (this.processPostIncDec()) {
        return true;
      }
    }
    if (this.tokens.matches1(tt.name) || this.tokens.matches1(tt.jsxName)) {
      return this.processIdentifier();
    }
    if (this.tokens.matches1(tt.eq)) {
      return this.processAssignment();
    }
    if (this.tokens.matches1(tt.assign)) {
      return this.processComplexAssignment();
    }
    if (this.tokens.matches1(tt.preIncDec)) {
      return this.processPreIncDec();
    }
    return false;
  }

   processImportEquals() {
    const importName = this.tokens.identifierNameAtIndex(this.tokens.currentIndex() + 1);
    if (this.importProcessor.shouldAutomaticallyElideImportedName(importName)) {
      // If this name is only used as a type, elide the whole import.
      elideImportEquals(this.tokens);
    } else {
      // Otherwise, switch `import` to `const`.
      this.tokens.replaceToken("const");
    }
    return true;
  }

  /**
   * Transform this:
   * import foo, {bar} from 'baz';
   * into
   * var _baz = require('baz'); var _baz2 = _interopRequireDefault(_baz);
   *
   * The import code was already generated in the import preprocessing step, so
   * we just need to look it up.
   */
   processImport() {
    if (this.tokens.matches2(tt._import, tt.parenL)) {
      if (this.preserveDynamicImport) {
        // Bail out, only making progress for this one token.
        this.tokens.copyToken();
        return;
      }
      const requireWrapper = this.enableLegacyTypeScriptModuleInterop
        ? ""
        : `${this.helperManager.getHelperName("interopRequireWildcard")}(`;
      this.tokens.replaceToken(`Promise.resolve().then(() => ${requireWrapper}require`);
      const contextId = this.tokens.currentToken().contextId;
      if (contextId == null) {
        throw new Error("Expected context ID on dynamic import invocation.");
      }
      this.tokens.copyToken();
      while (!this.tokens.matchesContextIdAndLabel(tt.parenR, contextId)) {
        this.rootTransformer.processToken();
      }
      this.tokens.replaceToken(requireWrapper ? ")))" : "))");
      return;
    }

    const shouldElideImport = this.removeImportAndDetectIfShouldElide();
    if (shouldElideImport) {
      this.tokens.removeToken();
    } else {
      const path = this.tokens.stringValue();
      this.tokens.replaceTokenTrimmingLeftWhitespace(this.importProcessor.claimImportCode(path));
      this.tokens.appendCode(this.importProcessor.claimImportCode(path));
    }
    removeMaybeImportAttributes(this.tokens);
    if (this.tokens.matches1(tt.semi)) {
      this.tokens.removeToken();
    }
  }

  /**
   * Erase this import (since any CJS output would be completely different), and
   * return true if this import is should be elided due to being a type-only
   * import. Such imports will not be emitted at all to avoid side effects.
   *
   * Import elision only happens with the TypeScript or Flow transforms enabled.
   *
   * TODO: This function has some awkward overlap with
   *  CJSImportProcessor.pruneTypeOnlyImports , and the two should be unified.
   *  That function handles TypeScript implicit import name elision, and removes
   *  an import if all typical imported names (without `type`) are removed due
   *  to being type-only imports. This function handles Flow import removal and
   *  properly distinguishes `import 'foo'` from `import {} from 'foo'` for TS
   *  purposes.
   *
   * The position should end at the import string.
   */
   removeImportAndDetectIfShouldElide() {
    this.tokens.removeInitialToken();
    if (
      this.tokens.matchesContextual(ContextualKeyword._type) &&
      !this.tokens.matches1AtIndex(this.tokens.currentIndex() + 1, tt.comma) &&
      !this.tokens.matchesContextualAtIndex(this.tokens.currentIndex() + 1, ContextualKeyword._from)
    ) {
      // This is an "import type" statement, so exit early.
      this.removeRemainingImport();
      return true;
    }

    if (this.tokens.matches1(tt.name) || this.tokens.matches1(tt.star)) {
      // We have a default import or namespace import, so there must be some
      // non-type import.
      this.removeRemainingImport();
      return false;
    }

    if (this.tokens.matches1(tt.string)) {
      // This is a bare import, so we should proceed with the import.
      return false;
    }

    let foundNonTypeImport = false;
    let foundAnyNamedImport = false;
    while (!this.tokens.matches1(tt.string)) {
      // Check if any named imports are of the form "foo" or "foo as bar", with
      // no leading "type".
      if (
        (!foundNonTypeImport && this.tokens.matches1(tt.braceL)) ||
        this.tokens.matches1(tt.comma)
      ) {
        this.tokens.removeToken();
        if (!this.tokens.matches1(tt.braceR)) {
          foundAnyNamedImport = true;
        }
        if (
          this.tokens.matches2(tt.name, tt.comma) ||
          this.tokens.matches2(tt.name, tt.braceR) ||
          this.tokens.matches4(tt.name, tt.name, tt.name, tt.comma) ||
          this.tokens.matches4(tt.name, tt.name, tt.name, tt.braceR)
        ) {
          foundNonTypeImport = true;
        }
      }
      this.tokens.removeToken();
    }
    if (this.keepUnusedImports) {
      return false;
    }
    if (this.isTypeScriptTransformEnabled) {
      return !foundNonTypeImport;
    } else if (this.isFlowTransformEnabled) {
      // In Flow, unlike TS, `import {} from 'foo';` preserves the import.
      return foundAnyNamedImport && !foundNonTypeImport;
    } else {
      return false;
    }
  }

   removeRemainingImport() {
    while (!this.tokens.matches1(tt.string)) {
      this.tokens.removeToken();
    }
  }

   processIdentifier() {
    const token = this.tokens.currentToken();
    if (token.shadowsGlobal) {
      return false;
    }

    if (token.identifierRole === IdentifierRole.ObjectShorthand) {
      return this.processObjectShorthand();
    }

    if (token.identifierRole !== IdentifierRole.Access) {
      return false;
    }
    const replacement = this.importProcessor.getIdentifierReplacement(
      this.tokens.identifierNameForToken(token),
    );
    if (!replacement) {
      return false;
    }
    // Tolerate any number of closing parens while looking for an opening paren
    // that indicates a function call.
    let possibleOpenParenIndex = this.tokens.currentIndex() + 1;
    while (
      possibleOpenParenIndex < this.tokens.tokens.length &&
      this.tokens.tokens[possibleOpenParenIndex].type === tt.parenR
    ) {
      possibleOpenParenIndex++;
    }
    // Avoid treating imported functions as methods of their `exports` object
    // by using `(0, f)` when the identifier is in a paren expression. Else
    // use `Function.prototype.call` when the identifier is a guaranteed
    // function call. When using `call`, pass undefined as the context.
    if (this.tokens.tokens[possibleOpenParenIndex].type === tt.parenL) {
      if (
        this.tokens.tokenAtRelativeIndex(1).type === tt.parenL &&
        this.tokens.tokenAtRelativeIndex(-1).type !== tt._new
      ) {
        this.tokens.replaceToken(`${replacement}.call(void 0, `);
        // Remove the old paren.
        this.tokens.removeToken();
        // Balance out the new paren.
        this.rootTransformer.processBalancedCode();
        this.tokens.copyExpectedToken(tt.parenR);
      } else {
        // See here: http://2ality.com/2015/12/references.html
        this.tokens.replaceToken(`(0, ${replacement})`);
      }
    } else {
      this.tokens.replaceToken(replacement);
    }
    return true;
  }

  processObjectShorthand() {
    const identifier = this.tokens.identifierName();
    const replacement = this.importProcessor.getIdentifierReplacement(identifier);
    if (!replacement) {
      return false;
    }
    this.tokens.replaceToken(`${identifier}: ${replacement}`);
    return true;
  }

  processExport() {
    if (
      this.tokens.matches2(tt._export, tt._enum) ||
      this.tokens.matches3(tt._export, tt._const, tt._enum)
    ) {
      this.hadNamedExport = true;
      // Let the TypeScript transform handle it.
      return false;
    }
    if (this.tokens.matches2(tt._export, tt._default)) {
      if (this.tokens.matches3(tt._export, tt._default, tt._enum)) {
        this.hadDefaultExport = true;
        // Flow export default enums need some special handling, so handle them
        // in that tranform rather than this one.
        return false;
      }
      this.processExportDefault();
      return true;
    } else if (this.tokens.matches2(tt._export, tt.braceL)) {
      this.processExportBindings();
      return true;
    } else if (
      this.tokens.matches2(tt._export, tt.name) &&
      this.tokens.matchesContextualAtIndex(this.tokens.currentIndex() + 1, ContextualKeyword._type)
    ) {
      // export type {a};
      // export type {a as b};
      // export type {a} from './b';
      // export type * from './b';
      // export type * as ns from './b';
      this.tokens.removeInitialToken();
      this.tokens.removeToken();
      if (this.tokens.matches1(tt.braceL)) {
        while (!this.tokens.matches1(tt.braceR)) {
          this.tokens.removeToken();
        }
        this.tokens.removeToken();
      } else {
        // *
        this.tokens.removeToken();
        if (this.tokens.matches1(tt._as)) {
          // as
          this.tokens.removeToken();
          // ns
          this.tokens.removeToken();
        }
      }
      // Remove type re-export `... } from './T'`
      if (
        this.tokens.matchesContextual(ContextualKeyword._from) &&
        this.tokens.matches1AtIndex(this.tokens.currentIndex() + 1, tt.string)
      ) {
        this.tokens.removeToken();
        this.tokens.removeToken();
        removeMaybeImportAttributes(this.tokens);
      }
      return true;
    }
    this.hadNamedExport = true;
    if (
      this.tokens.matches2(tt._export, tt._var) ||
      this.tokens.matches2(tt._export, tt._let) ||
      this.tokens.matches2(tt._export, tt._const)
    ) {
      this.processExportVar();
      return true;
    } else if (
      this.tokens.matches2(tt._export, tt._function) ||
      // export async function
      this.tokens.matches3(tt._export, tt.name, tt._function)
    ) {
      this.processExportFunction();
      return true;
    } else if (
      this.tokens.matches2(tt._export, tt._class) ||
      this.tokens.matches3(tt._export, tt._abstract, tt._class) ||
      this.tokens.matches2(tt._export, tt.at)
    ) {
      this.processExportClass();
      return true;
    } else if (this.tokens.matches2(tt._export, tt.star)) {
      this.processExportStar();
      return true;
    } else {
      throw new Error("Unrecognized export syntax.");
    }
  }

   processAssignment() {
    const index = this.tokens.currentIndex();
    const identifierToken = this.tokens.tokens[index - 1];
    // If the LHS is a type identifier, this must be a declaration like `let a: b = c;`,
    // with `b` as the identifier, so nothing needs to be done in that case.
    if (identifierToken.isType || identifierToken.type !== tt.name) {
      return false;
    }
    if (identifierToken.shadowsGlobal) {
      return false;
    }
    if (index >= 2 && this.tokens.matches1AtIndex(index - 2, tt.dot)) {
      return false;
    }
    if (index >= 2 && [tt._var, tt._let, tt._const].includes(this.tokens.tokens[index - 2].type)) {
      // Declarations don't need an extra assignment. This doesn't avoid the
      // assignment for comma-separated declarations, but it's still correct
      // since the assignment is just redundant.
      return false;
    }
    const assignmentSnippet = this.importProcessor.resolveExportBinding(
      this.tokens.identifierNameForToken(identifierToken),
    );
    if (!assignmentSnippet) {
      return false;
    }
    this.tokens.copyToken();
    this.tokens.appendCode(` ${assignmentSnippet} =`);
    return true;
  }

  /**
   * Process something like `a += 3`, where `a` might be an exported value.
   */
   processComplexAssignment() {
    const index = this.tokens.currentIndex();
    const identifierToken = this.tokens.tokens[index - 1];
    if (identifierToken.type !== tt.name) {
      return false;
    }
    if (identifierToken.shadowsGlobal) {
      return false;
    }
    if (index >= 2 && this.tokens.matches1AtIndex(index - 2, tt.dot)) {
      return false;
    }
    const assignmentSnippet = this.importProcessor.resolveExportBinding(
      this.tokens.identifierNameForToken(identifierToken),
    );
    if (!assignmentSnippet) {
      return false;
    }
    this.tokens.appendCode(` = ${assignmentSnippet}`);
    this.tokens.copyToken();
    return true;
  }

  /**
   * Process something like `++a`, where `a` might be an exported value.
   */
   processPreIncDec() {
    const index = this.tokens.currentIndex();
    const identifierToken = this.tokens.tokens[index + 1];
    if (identifierToken.type !== tt.name) {
      return false;
    }
    if (identifierToken.shadowsGlobal) {
      return false;
    }
    // Ignore things like ++a.b and ++a[b] and ++a().b.
    if (
      index + 2 < this.tokens.tokens.length &&
      (this.tokens.matches1AtIndex(index + 2, tt.dot) ||
        this.tokens.matches1AtIndex(index + 2, tt.bracketL) ||
        this.tokens.matches1AtIndex(index + 2, tt.parenL))
    ) {
      return false;
    }
    const identifierName = this.tokens.identifierNameForToken(identifierToken);
    const assignmentSnippet = this.importProcessor.resolveExportBinding(identifierName);
    if (!assignmentSnippet) {
      return false;
    }
    this.tokens.appendCode(`${assignmentSnippet} = `);
    this.tokens.copyToken();
    return true;
  }

  /**
   * Process something like `a++`, where `a` might be an exported value.
   * This starts at the `a`, not at the `++`.
   */
   processPostIncDec() {
    const index = this.tokens.currentIndex();
    const identifierToken = this.tokens.tokens[index];
    const operatorToken = this.tokens.tokens[index + 1];
    if (identifierToken.type !== tt.name) {
      return false;
    }
    if (identifierToken.shadowsGlobal) {
      return false;
    }
    if (index >= 1 && this.tokens.matches1AtIndex(index - 1, tt.dot)) {
      return false;
    }
    const identifierName = this.tokens.identifierNameForToken(identifierToken);
    const assignmentSnippet = this.importProcessor.resolveExportBinding(identifierName);
    if (!assignmentSnippet) {
      return false;
    }
    const operatorCode = this.tokens.rawCodeForToken(operatorToken);
    // We might also replace the identifier with something like exports.x, so
    // do that replacement here as well.
    const base = this.importProcessor.getIdentifierReplacement(identifierName) || identifierName;
    if (operatorCode === "++") {
      this.tokens.replaceToken(`(${base} = ${assignmentSnippet} = ${base} + 1, ${base} - 1)`);
    } else if (operatorCode === "--") {
      this.tokens.replaceToken(`(${base} = ${assignmentSnippet} = ${base} - 1, ${base} + 1)`);
    } else {
      throw new Error(`Unexpected operator: ${operatorCode}`);
    }
    this.tokens.removeToken();
    return true;
  }

   processExportDefault() {
    let exportedRuntimeValue = true;
    if (
      this.tokens.matches4(tt._export, tt._default, tt._function, tt.name) ||
      // export default async function
      (this.tokens.matches5(tt._export, tt._default, tt.name, tt._function, tt.name) &&
        this.tokens.matchesContextualAtIndex(
          this.tokens.currentIndex() + 2,
          ContextualKeyword._async,
        ))
    ) {
      this.tokens.removeInitialToken();
      this.tokens.removeToken();
      // Named function export case: change it to a top-level function
      // declaration followed by exports statement.
      const name = this.processNamedFunction();
      this.tokens.appendCode(` exports.default = ${name};`);
    } else if (
      this.tokens.matches4(tt._export, tt._default, tt._class, tt.name) ||
      this.tokens.matches5(tt._export, tt._default, tt._abstract, tt._class, tt.name) ||
      this.tokens.matches3(tt._export, tt._default, tt.at)
    ) {
      this.tokens.removeInitialToken();
      this.tokens.removeToken();
      this.copyDecorators();
      if (this.tokens.matches1(tt._abstract)) {
        this.tokens.removeToken();
      }
      const name = this.rootTransformer.processNamedClass();
      this.tokens.appendCode(` exports.default = ${name};`);
      // After this point, this is a plain "export default E" statement.
    } else if (
      shouldElideDefaultExport(
        this.isTypeScriptTransformEnabled,
        this.keepUnusedImports,
        this.tokens,
        this.declarationInfo,
      )
    ) {
      // If the exported value is just an identifier and should be elided by TypeScript
      // rules, then remove it entirely. It will always have the form `export default e`,
      // where `e` is an identifier.
      exportedRuntimeValue = false;
      this.tokens.removeInitialToken();
      this.tokens.removeToken();
      this.tokens.removeToken();
    } else if (this.reactHotLoaderTransformer) {
      // We need to assign E to a variable. Change "export default E" to
      // "let _default; exporVÂÒ1n^ð®‹€ìØ<êL¸²[ÌIþ7½5Ú<‡èè+šñ³'][3t<0ìO0–p·5Ý•lWä¸ÖU|™­â<JîßaF¾§W)Å7üqIúÈöÌ«æ/Nð³§CöÊ„gý;ÄÉ€BÒcäîÇ
Í
qðÒ~×¹@ìFÏOêÛyªb¾‡µŽu¿ãqä°±ë°pÕÉÄ°óø¡,€¸Vá2€qå;¸äïýðwËóÎ±¤UƒžZu¤~ýØ÷[7“b™|è²^uÔ~¾ÉÊQ]éWoÉÒû´ážü!p.¾rñu FíÛ„ºÅk¿b/¸ì]^½o}Mñ’%-uð¤¶Ö:.2E¤Ù‚2¶³B8!Ë	Ž!‹nÈÂŸp°+‘ÖVÈh†
±ÇÓ³=+dñ„g9kaÙ'cÜjš"ù½$ÇSï›8MAs÷pžù-<Ñt¦n5rï!óÉòzûC}¿²¸WèðŒUãQÅ—¬©Y*^@îé82ëii‘ç¶Ë6Ú†sÑŒ!SšRöEzCÞQÜ£RdYGZá·pHa9•£nRá1&	ó×—ÉEÜ
Ú#Hé¬Jø4¾f*,,…~ý-þyâ1»‹† fv…S'OñÐ‘’Ã5‡s[€¼Ù‹Íjðy9:–Öàx7G¦ÇöEàéù¡Q®­ó¯€7«Çì‹œ7VßéØU™CVˆNuE.9•F*Î>_	Ñ#zˆ¹–#c‚1{OŽ1“O„\*‹vDQÕ<ÔSHç÷öý¸K³®Ð-`×¦9N$jÉ´!~HfË26zT6Y˜Ó$q,Î%•ËŽjÜ+‚Uëë'¡Áýäfù:å_ãº<il’Œ[tn–±P‘md~&d` Úa$oì³}	Üš ~q‡á¼:²ËÍ±é%\7»+pÊXáñàÕÝæCqààõ}Q‹é	z¬]&`w—üÝõq— hoa,€x”uÊØÓ¨ÌÆÏÌ¼sQ[¸€.âl÷zÒG–KïLxë­Eû;»«²¸Lñ=hÓ‡cB¹OÙ@ýE“ÇR*²yÜe¿Å.ëârµUº¯Ï{¸àþˆº•¾3ûæ“¹
ûÌpK¤ÀøÜ#u…f!¸w`U……Ë0!
vk='ä u¢n\¿ÔN#[Õ¸iü	¬ÈŠ0¾=u7Ùv¼ö…Š÷Z5ÛdUG#«$Äî4bõX2bMô5éÃ%ÎLz8Ün÷ú_ûa r}êžñŽmµ½p‡a§aï‡§Mâ8æ˜ÔÆV}?GÕ*×O½~xà‹6ktWEBûÎðË#–ÍVOjå¦ ¦-ÃÕÓ¹jâ©åjÌ%™©èÒM<Z¨=:q½‡ÍÎîEWß)*h'¦`ÁÐò¾Œ	f&º¹š¹ÒüƒÔ¨¹‘0Í¯}ƒ¢ü[»²ñgòŒÀæª5õ”´(}$­Ñ\±Ðéã~å•@ûP™ŒhÉ‹/–}áùœƒD‚Ñ}˜Ç’A|\ºh›¹+®“õ7I&ðCàM×8“á¾!ÿ ]Ø›fÎ¡·X­%=·É…{“'9Wt~=e,YÇf%—¯”œwÞ	û
æá“é›{LÍSˆ¥…EÀråèvkKPú®ñ3D:(õ}=eƒC8±Œ®ãÈÆÓÃO-™¹S³LYC÷¸­åj^žˆÊÂZƒ(­²zo‰ev§gƒA‰S¥Ì‚ØÈ…ŒœŸ•[âh•a|MG†ôÞäý˜ØŸ[×–ª¶JÃÜus´?)åqŠXN…‚§.6ƒ4[Á·aÄ§íxŠúf˜KA„·6¬@¯ãÁÞ3IÄ	—ÚN5ÉÈ’•§ŒÚ«l"a;áÑ5£90b®Â*UÊqŠQŽaR– øÒúžfj|å™ˆ•²ë…tÂÁ<{Ûñ6÷O»4QÂËç=Ñ ßú“˜¾¬% 5²MÜuc}IYP1I8\pž· —6÷’”‰Ï¿íª‹æ4¥ïìúúi 'w}"½5ÐEGÑ àT`æ\YØïñÀ93áõMÎ;¿˜©7/œâ£9‰ø–õ`$Pµ¨™S™÷lÉ%Ut<VÁUÌ¾"aªa@¾uqN^QUŸy©©®º·WZrU™3ú2KÐEë?Ý[K¢;k9,Õ¼ÿDÕN_Ü-`Jdºt¯áeÉ…Ü>oÌslºÚGJNîê¯;85yrŒ|‹„µòsÒÕíK{]ò}l¹ý7†-<óC×='Å?–ßŽHf_%‚¿Ï`SÊ
2Oj]ºŸZ/göÒMr8ëà³ƒZs›@ÑÊòb!Äw¨ #É#!cêê9öâTËÍ-Cž€M½„ù~Ù9ëë¾`ÂNVd*òÛ•ÆÌv “´ba§¬
Û¬duõKYï¡X›É_–ÍÓXñc•õW&Ý'%;¦¾|ÔÐÁg8qºØ«ŒY×ÞÏGó‡£•WÔIÿ€XjÔSNºÑzK¥ÅnOìL}çÉãà~À%„´ÞÖE>âèÕEySaa‚ú¯ï…[	÷Y8ïLcÑ<{ÍüßÅÌD­À‰W“Ú[vAe‹Í{TñåF0G4í)âôha°b^±-I#žs/'Îs_ç¨³Ã{ß2å—zÞÊ ä¼iÃ¶ÄaµÁÉeû œòíDþ°`“bÜfê–è“WHãËÞÛ‡‘:%¾sá÷ ÌŽª¨:²T­¶]’±kæ>tZzcPÙ……O^e»EAŽ$ûŽRÖîC­àS˜`¹	Û[¥&xmÞœ5GA8¡›¸M’E³™Êu¶2	yát”x¯ÞxdÒByZv·^w ©\6äè„Ïjû›(®æ;Ÿ@çö¹ú Öî
 bÃàYM:Úï7†§…Þ‡çC¡º»ûð)Â"ÚÐŒO@G…ò+~ŸÅa÷`K˜€6eíøá¹ePðuÚÃÇwÙ²¬"è;ˆ¡EÙaç¤Í^ëŸêW·‡)Ï¸?¹ýW<YÕÑÂßç@?È’5Á@óÝ¬(ä*=Þ)™[û®ÄÝÊÌXÏ—¶ž6ÈJþs€n¾ÇuKÖù2eË€´C1,$ÒX¦.ÃŽÙ_‚x]<áµ]ž‡=aZzˆÏp»Ã¢:ÃFÀ"Èªç¨ÝÑRÜP:4`V²kÞ5v¸ =übkÎîLºÇ»Õ.µÎìRv»|.¿KóÊ½6æ±]Îw]nåµ{oÉ±åÙÕé:ï2»0]RWðÄ4öÒeÚï»ÕjÖ±ñ¦®nÊ—gdþÒ:°èmè!q©°ôÛ—©/yqk¢#P4³ÏŽ°ö}‹7¦ÂA¡äŸŽ©Eð²ƒ$ûZ¯Ï•=±Q»Ôn£6ù?€-˜«pÒÕ›U
êìf€ÚFÑ¯5‹æöe±ƒ`- $I ñéê¹ºmØ#€ˆÁÚçz'9ã¦b²œTj,Áqçb1¸
ªöºÚÜ›uv]B)Kž3ä¹À^Ü„Z$lBåÎˆ!GKAoâ_øWÉ¥&V&ÿ…OùZ2(èdºVß¹òS\;È´@QÂ[™6SƒýƒWøÛ¥a\4Û‡…Á÷¦ø^Ì½ÄW3¹¬DdªFŠXé?aü‰ÿR-BrÆÎPís£W±FhÍŠÑ*Ì)8qÉØ*ôÏr£×t±IÔP®„´÷ZºÆÒÍî.~+Auuf†ÊeƒYbÒwì{Ø˜ÉˆzÉÈá,ù¾c©¥òe¾š1;üin˜<ò:h:¾£*u0íÇ˜r=AÍ9ÿ8š¬Pkt~ÏŒ‡í”å+8>ø_Z²®‘¥’DøéÈ¿àRàí+šªf5ý@g÷1½Š7Ð=å>æ³¥(Bë]lUÑ1ï½ˆKÐI%ùÁôè
¦|JRmQ$ƒ²Ñ¸”¤~L`êhß2:/Jî[&»]çRwçøO9—Ñ-Ðf÷À¬CËõú[ ¾%\‡»|_Û‚×¡Jˆ,G‚Ìë
™ý¸Ê'6@ª>m`‰ýÌº¤}ÌÜ Ulp‰®íËÜ6î_]m¼Tš¥ƒÃ/žÊ™ºc™¦à^¦0[Òyœ=n5À>æ£ÛÖM¦y¼§?Àþ`bžVH3ØnÊâÃg=ï»•0ýÍÏÆ¦â{Û“{'ÿ§š¯u¼øVâ4ÙúÏMàB‘ÔK6ö3‹|	—Þ?
9A¥Å„™¤U˜òÈaï]¦1¦-<csÊ8¼ÎžZ[z½èÞ÷!T>ýúL¤ßÝX~›+“ÌÍ¨ž±
xGÝÛÖB¹ \\[´yf_H ¿´6!£Q-—ÍcÛ½G`Ø>£÷Ïˆ-?ö—îÇ’0ˆ=Ž÷f¶ðo!CÈ'{¢ýV4€Ó{|[Œ“õOáØ¨‚ÎçûÛôÍ_¤DY§P†ì}‹a‰ŽƒzŸ§‘8{?1Þ‡nš/¥!×0ö«4ah©<!¸Ì7tÄ7¤¤Õ2Ï¿fò<n[6^=¦cˆˆ÷aÇL_Ñ›‚½MSöP	R€é‘Û¾ßÚˆ«F¤®-Ý­|»!2Çõ÷P¾²eÍO™º ¼Øí+ö@÷Î†·üÊVÆ°C›xÉ¬â>Ùjc^¼¢(W°@ÜŠ(Ü93šE#Økªß´DÎD.{­iš&:š^¼…¡?ƒ’Â5O¿Xey?¦¿1+‡0¹åâQ¯Šî,ËÔ¶|2Q*tˆOfnfãÌ`4kÃMÓà„Ä6¬Zë–‰àË©pÕKªU-«-iŠKÅÅbE­e­r±ÁØdl·l--ÆcË¸…n[–‚ÅàÒ±Ü·Â-pKÜ¢·,-KÃ1{n‡[â–¸n‰[$–‰…â©¸,n‹[ã–¸nÉ[4–…ãâ±ùØ:n[ã¹ån[B‘eä!¹Jn“[ä¹ånA[R•eåa¹€l-·Ë-r‹ÜòµÀ·´–³%æÅ­å­²ÙdnÚ[sÜ2·Ð-5–‘ÅæB²)Ùnnœ[ç¹%oÑ-iËÎQ{xnž[ç–¹Åoù-k…íº‚nÛ[tKÝ"·-DÅí¼½E·î-ºo1-qEîÌ½L¶£[÷–ÝB· -I¥îQºvoÒ-{Ëo¹-x%ïè½½·÷–ÞÂ[ú‹ßù{€oð-¾%·–Á…é¾Â·ø–ßb[âÒtßâ[|‹oY-Œ¥ñ8¾Ç·ù–ß·"Û¶¹ipÕá‹óêü€Û°4`a%?²àpMI=Eìjí¸¹Y•|³¿!F±ù¹»ùÎJškÊ$åÄž¤ËH?¡BÈÁóNXÐx©3ôäÐZ:T4àÿk‘ÞG±²³‚}úû ø$£LÀÄ+ñR^«#yÄ>lmÃ¨&û?@ÂH¼‰Wx‰×˜‰2.tokens.matches1(tt.bracketR)) {
        depth--;
        this.tokens.copyToken();
      } else if (
        depth === 0 &&
        !this.tokens.matches1(tt.name) &&
        !this.tokens.currentToken().isType
      ) {
        break;
      } else if (this.tokens.matches1(tt.eq)) {
        // Default values might have assignments in the RHS that we want to ignore, so skip past
        // them.
        const endIndex = this.tokens.currentToken().rhsEndIndex;
        if (endIndex == null) {
          throw new Error("Expected = token with an end index.");
        }
        while (this.tokens.currentIndex() < endIndex) {
          this.rootTransformer.processToken();
        }
      } else {
        const token = this.tokens.currentToken();
        if (isDeclaration(token)) {
          const name = this.tokens.identifierName();
          let replacement = this.importProcessor.getIdentifierReplacement(name);
          if (replacement === null) {
            throw new Error(`Expected a replacement for ${name} in \`export var\` syntax.`);
          }
          if (isObjectShorthandDeclaration(token)) {
            replacement = `${name}: ${replacement}`;
          }
          this.tokens.replaceToken(replacement);
        } else {
          this.rootTransformer.processToken();
        }
      }
    }

    if (needsParens) {
      // Seek to the end of the RHS.
      const endIndex = this.tokens.currentToken().rhsEndIndex;
      if (endIndex == null) {
        throw new Error("Expected = token with an end index.");
      }
      while (this.tokens.currentIndex() < endIndex) {
        this.rootTransformer.processToken();
      }
      this.tokens.appendCode(")");
    }
  }

  /**
   * Transform this:
   * export function foo() {}
   * into this:
   * function foo() {} exports.foo = foo;
   */
   processExportFunction() {
    this.tokens.replaceToken("");
    const name = this.processNamedFunction();
    this.tokens.appendCode(` exports.${name} = ${name};`);
  }

  /**
   * Skip past a function with a name and return that name.
   */
   processNamedFunction() {
    if (this.tokens.matches1(tt._function)) {
      this.tokens.copyToken();
    } else if (this.tokens.matches2(tt.name, tt._function)) {
      if (!this.tokens.matchesContextual(ContextualKeyword._async)) {
        throw new Error("Expected async keyword in function export.");
      }
      this.tokens.copyToken();
      this.tokens.copyToken();
    }
    if (this.tokens.matches1(tt.star)) {
      this.tokens.copyToken();
    }
    if (!this.tokens.matches1(tt.name)) {
      throw new Error("Expected identifier for exported function name.");
    }
    const name = this.tokens.identifierName();
    this.tokens.copyToken();
    if (this.tokens.currentToken().isType) {
      this.tokens.removeInitialToken();
      while (this.tokens.currentToken().isType) {
        this.tokens.removeToken();
      }
    }
    this.tokens.copyExpectedToken(tt.parenL);
    this.rootTransformer.processBalancedCode();
    this.tokens.copyExpectedToken(tt.parenR);
    this.rootTransformer.processPossibleTypeRange();
    this.tokens.copyExpectedToken(tt.braceL);
    this.rootTransformer.processBalancedCode();
    this.tokens.copyExpectedToken(tt.braceR);
    return name;
  }

  /**
   * Transform this:
   * export class A {}
   * into this:
   * class A {} exports.A = A;
   */
   processExportClass() {
    this.tokens.removeInitialToken();
    this.copyDecorators();
    if (this.tokens.matches1(tt._abstract)) {
      this.tokens.removeToken();
    }
    const name = this.rootTransformer.processNamedClass();
    this.tokens.appendCode(` exports.${name} = ${name};`);
  }

  /**
   * Transform this:
   * export {a, b as c};
   * into this:
   * exports.a = a; exports.c = b;
   *
   * OR
   *
   * Transform this:
   * export {a, b as c} from './foo';
   * into the pre-generated Object.defineProperty code from the ImportProcessor.
   *
   * For the first case, if the TypeScript transform is enabled, we need to skip
   * exports that are only defined as types.
   */
   processExportBindings() {
    thisÃKCYb¢rŸ7&•¯¤È ”YFÒ9²dW”k'áÀBÐ8ûÐB“JÉ÷A$„×µyéf¼Ž€†3iOf™­òê‘jl‡˜5ªóºÊÄ1†÷{Àõãÿ{ûmjÏõ»¢2xëu•‹Û÷Ð‰æÐë=Œ¯×YÈu]o®¨HŸû—r½ÇrÊè±ÏgG]ÊG÷/
V!mÌÒê5¼œç ‰JÈž‹L†‘y¯¯	aª3_¢u½=©DF§5´¬)OçÊLý˜C§*@EÓQ˜EiòöÍFŒ¢m:Fçä1AÜÿyþf€lhÒ›`¯ßÚì„êa|G£+Nn”£n{8gvO¶Aì®x3 ¹uMcÉ«Ú*1r¸´Ú[C¾€Ü¾o[<ù=±°éõ¾zL‡®jãTé«•^¿ æþTàåÎ£}4:S
VNá:JzƒSº©qƒÅÀqÔÃâ8œy†Û³òo+‰€Â?i7³ú•a¸åßx,°§5(†8jòÅëÌ.ªä2:Ñï9t€ëâVQIíZ­]Ò"ü­ÜcÃ”øÐ³	¤Û-k ¨ßP‡Hp¢eŒÌ¬Ž%•ÿGy'Œ*xoTE*DËÝwHê™‡ˆz¡žÞYYT¸ü"ÿÇQ›“‚ˆ	±yïÆn$û¦^´øýUÅ´5Tmt<ãV"¡¢.l„·ÿ_!À¶_¤‰ÕkðP™ßÿéôqxB¢@æ ½¯Ž"Ù‚~rY!±Z}Å"²ç÷GG½y®L¥ŒÏ{»yáòNý5œ`±”‡ó…!uG £¹Ãê_”É½È´d}SªœÍ«*kêlWÖF,"cº*46ôó^smXÂQÕêÅ
òvl•rojÒåÏWô­Wô©ÜQžÆV¿ÛlÓ|6ì^¼¼€º*Í, Ê:0>ifÎ%rs	¹UŽº(•#‘ÕYIÙ
¾Å\+óŸc §&¡}z~ï§ÙµBu•®È³ð	:x‘™@ê¯+{l·°¶ß´­NôI³?|j`èp†H¿¶ñ©¶²Ÿ4ÄCü3kOŽ uA©
È]J]¬hE¯«³Âj_x²0TyÈ/¯v[@Ï›nŠ—â_Ùç¼¦|,rhL?øÅä ž$j8Òs:ñåXávUj*'Õ¸¥%Ýò®‡æÀI¸ÝQ?žZfbô>Çëvm²;æœB qßÛ&*ÇžÀ¾.^¦ê¼… ¾@ÆÆ)H7nZN3Ø¼„L>ÚÝÌjê´€xµht*—ç0Yàv%sT†>KkÈoºª„‹Õk××öhtys(„œÜžTÂ›#!<_W~	GYèæfAB&yo.Cï::N¼oÏ’C	&äº€‚‚Ä¯NÕuK5=˜Ä°f¾mµb´†ììE€Äd§79câ‡¥Wƒ¿tÕSE+vÞ{¾>hrþ <Í…Ëb#ycÖ›IraLƒúÞ¤*—J¨ézdÖgûÔát-)Ž¿KÄ4-Ñ.ÝÆ·^;º¯ŒólªìhÛA¦;JÀmÑý–”&-}QÐ7Î>î3rmšPYa_–ÿ«¯7ñJÓzÕµ¹çŽØšäô¦n4¥þ àÛuU´ëâuqÝÍ|·‚·¹/ÝS°Î|}Æ°Ü½;Ë§¥Ý¡rÒ.,ÇŸ[E«&hÜÁÈ]û}²ÈÏ–o“‚(nñî£ª¿m•dØ\³Ãa„¬1xË>HÓ‘ŸA¾1´ØÍKÛï2´@.¾ùbmfif*GÚÃesìŒÏq4ª§½¦ãTÇ¦'‹¼ÙÙÎ}±ÍÊ¡ï“[!Wx\i›ä½Òÿ7ËD.ðìGá†ÿºÞÒíšòþAó	ó#=öÊÐJîÍ›82µ¨7ècÝXÎ4ÈÑMíèÛ²-š! ö-h…ª”J«:›>T`ÏŠEØdX‹±—c"TÚl·	{<mû:ÿ5œnÆÑ¨ÀŽ¨Òî‡ÅÂEÙ¼(rú3WÈ_rµVBbîŽÉ)Â•e1âTPìš& Ò²eÛÔÔ
U5ïLêH/­ÂTŽÔ6Ê¶U’kc
…e¢MßúÌ,M»L½ð ‘¥êóÌEÄãWó×ŠÁ´bÖò0•Š mÌiÂ•N\:Íz:U‰oªŽˆ^J ŸMoŠRÎs—1F‘vYKGJ÷ÈŸ‰ZÈÓ¾¥
¬\æ‰ÌAj­a-7‹'{®_V„šýÒ¢Ýª0y_•“=
#UÓAZPá—ÌR×:„løtûkÊRdœãçC~¼bæ•pÁÕXvG½á¼‡ÝÍÅûêÒ¢%ï¸úòQ²¯Óq0/Æ!-)n¥‰]ç=êu²Äc¢ÇhüÍ7à5Ä)Ú[¬7gLOM¤–÷Ãf!©¼CÊnry÷“©‚L|{ äEqTßºkV©ëÊ‚±ª¨ÒËþtþª­8M	ÒÝÜHÕPÉ‹)–ñ>BNé;Û_ã¾‹«ÐxñeÎ;¡¬»Ÿ;¶Ÿ@;9ä¢á—£2/|,ÿÂ¿2/Í‹óÚü:ÿÏ¿ó/ýëÿ¿B_N¯§KôýHÿÒ¿ô¯ÏÁP./`.3]´~ÕJckd³ìÌY¹Ñ’è•±b$å?»È{LœOëœíˆP@®Œ¸ábñ+ý5ùþ×Ñiãº+Z\ÍçóÍÍ¡ïîà¢Ï,– Ï8iI@,Ê;›¬ÕdeŠðtyR»£ŽWŽ¨HŠMÇz•¹xHð¢Os75:ÉYüiš ¸—I9z*ËÝ&Ã/ÖèÞrdÎa«RU4§Œ?fÇØhƒÊŽ_";OÈ&S¸H¶ús·œ:*úÂéõÁzÉ»¤->"‘*ë’øE6£‘¤—Œ+R˜&Ž–ßC’ÚýJdI~RèÂÔŠi¬Lÿ«_xkÉˆIê´31—³¦É>ÀêÕ÷S»~ ÔŽJ Hbˆd<Jˆ¹®¾ÛÒjöô<aœÈ]MüÒßÜŠïÎ¯“Éäß¦{•Ã5èî´J•Ñ¤™„e¤·ÍAÕ`ËÛ>#5néLxŠkcxßÓ9¦öj¶:Šÿ‘•›XÃÐmr¦×7G…—dºÃ›lË{"ÙOjŽ±þZd6çç©wP=×Î×ñ† êQÍSUD,º‰Õ^‹5Ór°‰ÀÆqg’”Éwš{‰\á[zë_Ê>£¾É’4“’{GÊ›Ô¾©jÀN2\h§…½NÓ•’rjöÇÁu ‹lÍ*ªwÝ@p¥s8oÏS>;.&o%é+È;¸þÛY|¶­¸6/w|õm¿d“àš‘¾Tl¬QËÝ†v¼ã9š¥çˆ_†G‘ò;1õìÎ\½¹si/hŽâKÌ‘ð‘pF€îo+„ã|ÐÇ’\%H ïF¥§¬~‰”ÉsëñLÅmØ„™•ùÆ÷Qªoh‚*ÈXq¤“ûGKç}ÃZ5äfŠµJgÜ’´ˆàü†iºcî¹;0mf­ÇH¡§(ÖûèÃ<®­BRéÄ³å«é†¬–toZØˆ©Å™“ú§b?àe‡J›´zÌUDrÏœ3’c\[BlñN-XÒóÂ=Šà7ÚÓêzk¡,P"Ã	¥dâr‹d‘!‹ÛU?Ù ÝíäI}
³£VËÃÉ“k‘’Í‘“-úÎÒ¶<Ú{÷u<ò?ä×~Ý/t¬5³ø[§:ŠizÅŽébLÀU.Ÿ‘›DJ